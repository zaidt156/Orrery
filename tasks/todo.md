# Site consistency — task list

- [x] **1. Landing page: Download → Install.** Nav, hero button, and section renamed together;
      release cards replaced with the one-line install; dead `#install-and-run` anchor fixed.
      *Done when:* no `/releases` link remains, every in-page anchor resolves, command matches README.
- [x] **2. Landing page: correct stale claims.** Automations "in progress" is canvas editing only;
      drop the closed-P0 security sentence.
      *Done when:* nothing on the page contradicts TODO.md or ARCHITECTURE.md.
- [x] **CHECKPOINT** — links resolve and the page renders before moving to the guide.
- [x] **3. Guide §02: one-line install.** Replace the eight-command block; state what the first run
      does for you; keep the launch-code paragraph.
      *Done when:* the guide's command matches the README's exactly.
- [x] **4. Guide 03–12: reality pass.** Fix only what is now false.
      *Done when:* no reference to a separate window or to setup the app performs itself.
- [x] **CHECKPOINT** — read both pages end to end as a new user.

All tasks completed 18 August 2026. Verified: every in-page anchor resolves on both pages, both cross-page anchors resolve, no `/releases` link remains, the install command matches README.md character for character, and the landing page parses with no unclosed or mismatched tags.
